import 'package:flutter/material.dart';

class GridViews extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red,
        title: Text('Grid Views'),
      ),
      body: Center(
        child: SizedBox(
          height: 10.0,
        ),
      ),
    );
  }
}